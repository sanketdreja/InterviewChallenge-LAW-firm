import { createTheme } from "@mui/material";
const theme = createTheme({
  palette: {
    ochre: {
      main: "#e3b748",
      light: "#ffffff",
      dark: "#111111",
      contrastText: "#474747",
    },
  },
});

export default theme;
