import { createTheme } from "@mui/material";
const theme = createTheme({
  palette: {
    ochre: {
      main: "#e3b748",
      //   light: "#E9DB5D",
      //   dark: "#A29415",
      //   contrastText: "#242105",
    },
    white: { main: "#ffffff" },
    black: { main: "#111111" },
  },
});

export default theme;
